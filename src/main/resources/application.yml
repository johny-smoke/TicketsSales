spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5433/TicketSales'
    username: 'postgres'
    password: 'Postgres098'
    driver-class-name: 'org.postgresql.Driver'
  jpa:
    show-sql: true
logging:
  level:
    org.springframework.web: debug